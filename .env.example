# Baby Tracker Configuration
# Copy this file to .env and adjust as needed.

# --- Go API Server ---
# Port the API server listens on (default: 8080)
PORT=8080

# Absolute path for data storage (default: ~/.babytracker)
# DATA_DIR=/path/to/data

# Desktop window title (default: Baby Tracker)
# APP_TITLE=Baby Tracker

# --- React Web App ---
# API base URL the web frontend connects to (default: http://localhost:8080/api)
# REACT_APP_API_BASE=http://localhost:8080/api

# Port the React dev server runs on (default: 3000)
# PORT=3000  (set this in web/.env, not here — CRA reads its own PORT)
